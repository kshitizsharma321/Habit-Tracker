// Streak lengths (in days) that trigger a one-time celebration on the Track page.
// Dev-defined — keep these sparse so the celebration stays special.
export const CELEBRATION_MILESTONES = [7, 10, 30, 60, 100];
